# 1) Create a Google Cloud OAuth client (Web application) and set a redirect URI.
# 2) Run the one-time OAuth flow to get a refresh token (see notes in the assistant message).
# 3) Put the token and settings here.

PORT=8787
TIMEZONE=America/New_York

GOOGLE_CLIENT_ID=YOUR_CLIENT_ID
GOOGLE_CLIENT_SECRET=YOUR_CLIENT_SECRET
GOOGLE_REDIRECT_URI=http://localhost:8787/oauth2callback

# This is the key that actually lets the server book into your calendar:
GOOGLE_REFRESH_TOKEN=YOUR_REFRESH_TOKEN

# Calendar you want to book into:
GOOGLE_CALENDAR_ID=primary

# If availability blocks don't have an end time, assume this duration:
BOOKING_DURATION_MINUTES=60

# If you want the server to delete the clicked availability block after booking:
DELETE_AVAILABILITY=false
